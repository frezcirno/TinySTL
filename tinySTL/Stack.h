#pragma once
#include <stack>
template <typename T>
class Stack {
   public:
    Stack();
    ~Stack();
    void push_back(const T& elem);
    void pop_back();
   private:
};
int main(int argc, char const *argv[])
{
    std::stack<int>s;
    stack
    return 0;
}

